{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_AllowedHosts": "Host header allow-list. '*' accepts any Host, which permits host-header injection and cache poisoning. Override with the AllowedHosts app setting when the hostname changes; a wrong value makes every request 400, and the CI smoke test catches that because it boots with ASPNETCORE_ENVIRONMENT=Production against localhost.",
  "AllowedHosts": "app-lottery-8e49d22b.azurewebsites.net;localhost;127.0.0.1;[::1]",
  "Database": {
    "Provider": "Sqlite"
  }
}
